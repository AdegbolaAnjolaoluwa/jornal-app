{
  "name": "say-so",
  "version": "1.0.0",
  "description": "Configurable AI-powered capture and extraction system for user input",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static frontend, serverless backend ready'",
    "test": "node --test 'api/**/*.test.js'"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.24.0",
    "@vercel/postgres": "^0.8.0",
    "bcryptjs": "^2.4.3",
    "cookie": "^0.6.0",
    "jsonwebtoken": "^9.1.2"
  },
  "devDependencies": {
    "node": "^20.0.0"
  }
}
