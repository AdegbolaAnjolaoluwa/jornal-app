{
  "name": "say-so",
  "version": "1.0.0",
  "description": "Configurable AI-powered capture and extraction system for user input",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-sw.mjs",
    "test": "node --test",
    "start:local": "bash scripts/dev.sh"
  },
  "dependencies": {
    "@vercel/postgres": "^0.10.0",
    "bcryptjs": "^2.4.3",
    "cookie": "^2.0.1",
    "groq-sdk": "^1.3.0",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "node": "^20.0.0"
  }
}
