# Database
DATABASE_URL=postgresql://user:password@localhost/sayso

# Authentication
JWT_SECRET=your-secret-key-here
# Default session: short-lived, backs a browser-session-only cookie (no Max-Age).
JWT_EXPIRES_IN=1d
# "Remember me" checkbox on sign-in: long-lived, backs a persistent 30-day cookie.
JWT_EXPIRES_IN_REMEMBER_ME=30d

# AI
GROQ_API_KEY=your-api-key-here
GROQ_MODEL=llama-3.3-70b-versatile

# Reminders cron (Authorization: Bearer <value> required on /api/cron/reminders)
CRON_SECRET=your-cron-secret-here

# Application
APP_NAME=Say So
APP_DESCRIPTION=Capture and extract actionable insights from your work input

# Environment
NODE_ENV=development
